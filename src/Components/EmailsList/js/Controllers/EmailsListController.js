define(function (require) {
    'use strict';

    var EmailsListController = function (model, view) {
        this._model = model;
        this._view = view;
    };

    EmailsListController.prototype = (function () {
        
        return {}
    })();

    return EmailsListController;
});
