define(function () {
    
    'use strict';

    var MenuController = function (model, view) {
        this._model = model;
        this._view = view;
    };

    MenuController.prototype = (function () {
       
       return {}
    })();

    return MenuController;
});

